import { IDDay } from 'types/dday';

interface IProps {
  item: IDDay;
}

const DDayContainer = ({ item }: IProps) => {
  return <div>디데이 컨테이너지롱</div>;
};

export default DDayContainer;

// TODO: DDay container 완성
